server:
  port: 9100

spring:
  application:
    name: product-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8500

application:
  project:
    name: product-service

logging:
  path:
    root: ./logs